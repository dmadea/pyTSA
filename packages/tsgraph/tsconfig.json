{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "lib",
    "rootDir": "src"                                   /* Specify an output folder for all emitted files. */
  }
}
